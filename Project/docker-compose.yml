version: '3.9'
services:
  php:
    image: wodby/php:latest
    volumes:
      - './:/var/www/html'
    depends_on:
      - mariadb

  nginx:
    image: nginx:latest
    ports:
      - 88:80 
    links:
      - 'php'
    volumes:
      - './:/var/www/html'
      - './docker/nginx:/etc/nginx/conf.d'
    depends_on:
      - php

  mariadb:
    image: mariadb:latest
    environment:
      MYSQL_DATABASE: weer
      MYSQL_USER: weeruser
      MYSQL_PASSWORD: weerUserp@$$w0rd
      MYSQL_ROOT_PASSWORD: bfduvxijkn367GYUB_uibfesd#
    ports:
      - 3308:3306

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      PMA_HOST: mariadb
      PMA_USER: root
      PMA_PASSWORD: bfduvxijkn367GYUB_uibfesd#
    ports:
    - "1088:80"